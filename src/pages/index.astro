---
import Layout from "../layouts/Layout.astro";
import Topbar from "../components/Topbar.astro";

const user = Astro.locals.user;

// If user is logged in, redirect to /generate
if (user) {
  return Astro.redirect("/generate");
}
---

<Layout title="10x Cards - Inteligentne fiszki">
  <Topbar user={user} />

  <div class="min-h-screen bg-gradient-to-br from-indigo-900 via-purple-900 to-blue-900">
    <div class="container mx-auto px-4 py-16">
      <div class="max-w-4xl mx-auto text-center space-y-8">
        <!-- Hero Section -->
        <div class="space-y-4">
          <h1 class="text-5xl md:text-6xl font-bold text-white mb-4">10x Cards</h1>
          <p class="text-xl md:text-2xl text-blue-100/90">
            Generuj inteligentne fiszki z dowolnego tekstu za pomocą AI
          </p>
        </div>

        <!-- Features -->
        <div class="grid md:grid-cols-3 gap-6 mt-12">
          <div class="bg-white/10 backdrop-blur-lg rounded-xl p-6 border border-white/10">
            <div class="text-4xl mb-4">🤖</div>
            <h3 class="text-xl font-semibold text-white mb-2">AI-Powered</h3>
            <p class="text-blue-100/80">Automatyczne generowanie fiszek z dowolnego tekstu</p>
          </div>

          <div class="bg-white/10 backdrop-blur-lg rounded-xl p-6 border border-white/10">
            <div class="text-4xl mb-4">✏️</div>
            <h3 class="text-xl font-semibold text-white mb-2">Edycja</h3>
            <p class="text-blue-100/80">Pełna kontrola nad wygenerowanymi fiszkami</p>
          </div>

          <div class="bg-white/10 backdrop-blur-lg rounded-xl p-6 border border-white/10">
            <div class="text-4xl mb-4">💾</div>
            <h3 class="text-xl font-semibold text-white mb-2">Twoja baza</h3>
            <p class="text-blue-100/80">Wszystkie fiszki zapisane w Twoim koncie</p>
          </div>
        </div>

        <!-- CTA -->
        <div class="mt-12 space-y-4">
          <div class="flex gap-4 justify-center flex-wrap">
            <a
              href="/register"
              class="inline-flex items-center justify-center rounded-md text-lg font-medium bg-white text-indigo-900 h-12 px-8 py-3 hover:bg-blue-50 transition-colors shadow-lg"
            >
              Rozpocznij za darmo
            </a>
            <a
              href="/login"
              class="inline-flex items-center justify-center rounded-md text-lg font-medium bg-white/10 text-white h-12 px-8 py-3 hover:bg-white/20 transition-colors border border-white/20"
            >
              Zaloguj się
            </a>
          </div>
          <p class="text-sm text-blue-100/70">Bez karty kredytowej. Bez zobowiązań.</p>
        </div>
      </div>
    </div>
  </div>
</Layout>
